name: Deploy

on:
  push:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]
  
    steps:
    - name: Install OpenSSH
      run: |
        sudo apt-get install openssh-client

    - name: Open SSH
      run: |
        pwd
        cd .github/workflows
        ssh -i "key.pem" ubuntu@ec2-54-252-206-212.ap-southeast-2.compute.amazonaws.com